import discord

from discord.ext import commands
from utils import permissions, default


class Admin_Panel:
    def __init__(self, bot):
        self.bot = bot
        self.config = default.get("config.json")


def setup(bot):
    bot.add_cog(Admin_Panel(bot))
